import 'package:flutter/material.dart';

class AppColors {
  // Primary: Navy Blue
  static const Color primary = Color(0xFF0B1F4F);
  
  // Secondary: Light Gray (Background)
  static const Color secondary = Color(0xFFEBEFF3);
  
  // Accent: Yellow/Orange
  static const Color accent = Color(0xFFF9A825);

  // Tambahan untuk status
  static const Color success = Color(0xFF4CAF50); // Hijau Absen
  static const Color error = Color(0xFFE53935);   // Merah Absen/Error
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
}